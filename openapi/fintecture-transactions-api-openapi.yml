openapi: 3.2.0
info:
  title: Fintecture Transactions API
  version: v2
  contact:
    name: Fintecture Support
    url: https://fintecture.com/contact
  description: 'Operations tagged Transactions across 2 of this provider''s published API definitions: fintecture-ais-api-openapi.yml, fintecture-transactions-api-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://api.fintecture.com
  description: Production
- url: https://api-sandbox.fintecture.com
  description: Sandbox
security:
- BearerAuth: []
tags:
- name: Transactions
  description: Account transactions
paths:
  /ais/v1/customer/{customer_id}/account/{account_id}/transactions:
    get:
      summary: Get Account Transactions
      description: Returns all transactions linked to the requested account.
      operationId: getAisV1CustomerAccountTransactions
      tags:
      - Transactions
      parameters:
      - $ref: '#/components/parameters/CustomerIdPath'
      - $ref: '#/components/parameters/AccountIdPath'
      - in: query
        name: from
        schema:
          type: string
          format: date
      - in: query
        name: to
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Transaction list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionList'
    servers:
    - url: https://api.fintecture.com
      description: Production
    - url: https://api-sandbox.fintecture.com
      description: Sandbox
  /v1/transactions:
    servers:
    - url: https://api.fintecture.com
      description: Production
    - url: https://api-sandbox.fintecture.com
      description: Sandbox
    get:
      summary: List All Transactions
      operationId: listAllTransactions
      tags:
      - Transactions
      parameters:
      - in: query
        name: filter[from]
        schema:
          type: string
          format: date
      - in: query
        name: filter[to]
        schema:
          type: string
          format: date
      - in: query
        name: filter[type]
        schema:
          type: string
          enum:
          - debit
          - credit
      responses:
        '200':
          description: Transactions list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/MerchantTransaction'
  /v1/transactions/{transaction_id}:
    servers:
    - url: https://api.fintecture.com
      description: Production
    - url: https://api-sandbox.fintecture.com
      description: Sandbox
    get:
      summary: Get Specific Transaction
      operationId: getTransactionById
      tags:
      - Transactions
      parameters:
      - in: path
        name: transaction_id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Transaction details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MerchantTransaction'
components:
  schemas:
    Transaction:
      type: object
      properties:
        id:
          type: string
        amount:
          type: string
        currency:
          type: string
        booking_date:
          type: string
          format: date
        value_date:
          type: string
          format: date
        description:
          type: string
        type:
          type: string
          enum:
          - debit
          - credit
        counterparty:
          type: object
          properties:
            name:
              type: string
            iban:
              type: string
    TransactionList:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        meta:
          type: object
          properties:
            count:
              type: integer
    MerchantTransaction:
      type: object
      properties:
        id:
          type: string
        amount:
          type: string
        currency:
          type: string
        type:
          type: string
          enum:
          - debit
          - credit
        status:
          type: string
        booking_date:
          type: string
          format: date
        value_date:
          type: string
          format: date
        description:
          type: string
        related_payment_id:
          type: string
        related_settlement_id:
          type: string
  parameters:
    AccountIdPath:
      in: path
      name: account_id
      required: true
      schema:
        type: string
    CustomerIdPath:
      in: path
      name: customer_id
      required: true
      schema:
        type: string
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
x-refined-from:
- fintecture-ais-api-openapi.yml
- fintecture-transactions-api-openapi.yml
